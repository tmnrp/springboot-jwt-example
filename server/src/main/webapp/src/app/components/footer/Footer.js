import './Footer.scss';
import React, { useEffect } from 'react';

const Footer = (props) => {
    return (
        <div id="tm-footer">
            I am footer
        </div>
    );
};

export default Footer;