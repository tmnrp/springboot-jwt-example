import './LoginButton.scss';
import React from 'react';

const LoginButton = () => {
    return (
        <button id="tm-login-btn" className="tm-btn">
            Log in
        </button>
    );
};

export default LoginButton;